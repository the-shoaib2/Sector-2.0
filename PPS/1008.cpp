// the.shoaib2
#include <stdio.h>
int main()
{
    // SALARY
    int A, B;
    float C;
    scanf("%d %d %f", &A, &B, &C);
    printf("NUMBER = %d\n", A);
    printf("SALARY = U$ %0.2f\n", (B * C));
    return 0;
}